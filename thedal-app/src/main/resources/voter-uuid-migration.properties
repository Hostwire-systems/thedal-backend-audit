# UUID Migration Configuration
# Set to true to enable safe UUID operations during migration period
voter.uuid.migration.enabled=true

# Set to true to enable additional UUID validation logging
voter.uuid.validation.logging=true

# Timeout for family operations in milliseconds
voter.family.operation.timeout=30000
